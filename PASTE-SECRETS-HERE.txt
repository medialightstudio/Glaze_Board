# =============================================================================
# Secrets now live in .env.local (gitignored).
# =============================================================================
# Still needed — Neon HOST only:
#   Neon dashboard → Connection details → host
#   Example shape: ep-xxxxxxx.us-east-2.aws.neon.tech
#
# Reply in chat with just that host (or a connection URI with password REDACTED).
# Do not re-paste passwords or API keys.
# =============================================================================
